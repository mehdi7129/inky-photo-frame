---
phase: 03-module-extraction
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - inky_photo_frame/display.py
  - inky_photo_frame/image_processor.py
  - inky_photo_frame/photos.py
  - inky_photo_frame/buttons.py
  - inky_photo_frame/welcome.py
autonomous: true
requirements:
  - STRC-01

must_haves:
  truths:
    - "display.py contains DisplayManager singleton and retry_on_error decorator"
    - "image_processor.py contains process_image(), _apply_spectra_palette(), _apply_warmth_boost() as module-level functions"
    - "photos.py contains PhotoHandler class, get_all_photos(), and load_history() as module-level functions"
    - "buttons.py contains ButtonController class and BUTTONS_AVAILABLE flag"
    - "welcome.py contains display_welcome(), get_ip_address(), and get_credentials() as module-level functions"
    - "No circular imports exist between any extracted modules"
  artifacts:
    - path: "inky_photo_frame/display.py"
      provides: "DisplayManager singleton, retry_on_error decorator"
      contains: "class DisplayManager"
    - path: "inky_photo_frame/image_processor.py"
      provides: "Image processing pipeline functions"
      contains: "def process_image"
    - path: "inky_photo_frame/photos.py"
      provides: "PhotoHandler class and photo utility functions"
      contains: "class PhotoHandler"
    - path: "inky_photo_frame/buttons.py"
      provides: "ButtonController with GPIO handling"
      contains: "class ButtonController"
    - path: "inky_photo_frame/welcome.py"
      provides: "Welcome screen rendering functions"
      contains: "def display_welcome"
  key_links:
    - from: "inky_photo_frame/display.py"
      to: "inky_photo_frame/config.py"
      via: "from inky_photo_frame.config import VERSION"
      pattern: "from inky_photo_frame\\.config import"
    - from: "inky_photo_frame/image_processor.py"
      to: "inky_photo_frame/config.py"
      via: "from inky_photo_frame.config import SPECTRA_PALETTE, WARMTH_BOOST_CONFIG"
      pattern: "from inky_photo_frame\\.config import"
    - from: "inky_photo_frame/photos.py"
      to: "inky_photo_frame/config.py"
      via: "from inky_photo_frame.config import PHOTOS_DIR, HISTORY_FILE"
      pattern: "from inky_photo_frame\\.config import"
---

<objective>
Extract the five leaf modules from the monolith: display.py, image_processor.py, photos.py, buttons.py, welcome.py.

Purpose: These modules have dependencies only on config.py (extracted in 03-01), not on each other or on app.py. Extracting them before app.py ensures the orchestrator can import all its dependencies without circular references.

Output: Five Python modules in `inky_photo_frame/`, each containing the classes and functions identified in the research module extraction map.
</objective>

<execution_context>
@/Users/mehdiguiard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mehdiguiard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-module-extraction/03-RESEARCH.md
@.planning/phases/03-module-extraction/03-01-SUMMARY.md
@inky_photo_frame.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract display.py and image_processor.py</name>
  <files>inky_photo_frame/display.py, inky_photo_frame/image_processor.py</files>
  <action>
**display.py -- Extract from monolith lines 166-257:**

Create `inky_photo_frame/display.py` containing:
1. `DisplayManager` class (lines 166-227) -- the singleton with `__new__`, `initialize()`, `get_display()`, `cleanup()`
2. `retry_on_error()` decorator factory (lines 229-257)

**Imports for display.py:**
```python
import logging
import threading
import atexit
import signal
import time as time_module
from functools import wraps

from inky_photo_frame.config import VERSION
```

**Critical rules for display.py:**
- `from inky.auto import auto` stays INSIDE `DisplayManager.initialize()` method (lazy hardware import, NOT at module level)
- Import `VERSION` from `inky_photo_frame.config` (for the startup log message)
- Do NOT import `logging.basicConfig` or call `setup_logging()` -- display.py is not an entry point
- Preserve the `_lock = threading.Lock()` class variable
- Preserve the `atexit.register` and `signal.signal` calls inside `initialize()`
- Copy all docstrings and comments exactly

**image_processor.py -- Extract from monolith lines 758-880:**

Create `inky_photo_frame/image_processor.py` containing three functions extracted from `InkyPhotoFrame` methods, converted to module-level functions:

1. `_apply_spectra_palette(img)` (from lines 758-797) -- was `self._apply_spectra_palette(img)`. Remove `self` parameter. Uses `SPECTRA_PALETTE` from config instead of via `self`.

2. `_apply_warmth_boost(img)` (from lines 799-824) -- was `self._apply_warmth_boost(img)`. Remove `self` parameter. Uses `WARMTH_BOOST_CONFIG` from config instead of via `self`.

3. `process_image(image_path, width, height, color_mode, is_spectra)` (from lines 826-880) -- was `self.process_image(image_path)`. Remove `self` and take explicit parameters instead:
   - `image_path`: str or Path
   - `width`: int (display width)
   - `height`: int (display height)
   - `color_mode`: str ('pimoroni', 'spectra_palette', 'warmth_boost')
   - `is_spectra`: bool (whether display is Spectra model)

   Inside the function, replace `self.width` with `width`, `self.height` with `height`, `self.color_mode` with `color_mode`, `self.is_spectra` with `is_spectra`. Replace `self._apply_spectra_palette(img)` with `_apply_spectra_palette(img)` and `self._apply_warmth_boost(img)` with `_apply_warmth_boost(img)`.

**Imports for image_processor.py:**
```python
import logging
from pathlib import Path
from PIL import Image, ImageOps

from inky_photo_frame.config import SPECTRA_PALETTE, WARMTH_BOOST_CONFIG
```

Note: `ImageEnhance` is imported inside `_apply_spectra_palette()` and `_apply_warmth_boost()` locally (matching the monolith pattern). `ImageOps` may not be needed if not used -- check the monolith. If not used in these functions, omit it.

**Critical rules for image_processor.py:**
- These are now MODULE-LEVEL functions, not methods on a class
- `process_image()` takes explicit display parameters instead of reading from `self`
- The `from PIL import ImageEnhance` stays local inside the two color functions (matching monolith pattern)
- No hardware imports (no `inky`, no `gpiozero`)
  </action>
  <verify>
Run: `python3 -c "from inky_photo_frame.display import DisplayManager, retry_on_error; print('display.py OK')"` -- should print "display.py OK" (does NOT initialize hardware, just imports the class).

Run: `python3 -c "from inky_photo_frame.image_processor import process_image, _apply_spectra_palette, _apply_warmth_boost; print('image_processor.py OK')"` -- should print "image_processor.py OK".
  </verify>
  <done>display.py contains DisplayManager and retry_on_error. image_processor.py contains process_image(), _apply_spectra_palette(), _apply_warmth_boost() as module-level functions with explicit parameters. Both import from config.py only.</done>
</task>

<task type="auto">
  <name>Task 2: Extract photos.py, buttons.py, and welcome.py</name>
  <files>inky_photo_frame/photos.py, inky_photo_frame/buttons.py, inky_photo_frame/welcome.py</files>
  <action>
**photos.py -- Extract from monolith lines 346-401, 646-756:**

Create `inky_photo_frame/photos.py` containing:

1. `PhotoHandler` class (lines 346-400) -- the watchdog `FileSystemEventHandler` subclass. Keep constructor injection: `__init__(self, slideshow)` where `slideshow` is the app instance. Do NOT import `InkyPhotoFrame` or `app.py`.

2. `get_all_photos()` function (lines 646-655) -- extracted from `InkyPhotoFrame.get_all_photos()`. Convert to module-level function. Uses `PHOTOS_DIR` from config directly (no `self`).

3. `load_history()` function (lines 617-637) -- extracted from `InkyPhotoFrame.load_history()`. Convert to module-level function. Uses `HISTORY_FILE` from config directly.

**Imports for photos.py:**
```python
import json
import logging
import threading
from pathlib import Path
from watchdog.events import FileSystemEventHandler

from inky_photo_frame.config import PHOTOS_DIR, HISTORY_FILE
```

**Key changes from monolith:**
- `get_all_photos()`: replace `PHOTOS_DIR.glob(ext)` pattern -- same logic, just uses `PHOTOS_DIR` imported from config
- `load_history()`: replace `HISTORY_FILE.exists()` -- same logic, uses `HISTORY_FILE` from config
- `PhotoHandler.__init__`: receives `slideshow` via constructor injection. The `self.slideshow.add_to_queue()` and `self.slideshow.display_new_photo()` calls remain unchanged -- they call methods on the injected instance

**buttons.py -- Extract from monolith lines 52-56, 263-340:**

Create `inky_photo_frame/buttons.py` containing:

1. The guarded `gpiozero` import with `BUTTONS_AVAILABLE` flag (lines 52-56):
```python
try:
    from gpiozero import Button
    BUTTONS_AVAILABLE = True
except ImportError:
    BUTTONS_AVAILABLE = False
```

2. `ButtonController` class (lines 263-340) -- constructor injection pattern: `__init__(self, photo_frame)` where `photo_frame` is the app instance. Do NOT import `InkyPhotoFrame` or `app.py`.

**Imports for buttons.py:**
```python
import logging
```

(plus the guarded `gpiozero` import above)

**Critical rule:** `buttons.py` does NOT import from `app.py`. It receives the `photo_frame` instance via constructor injection. This is the CORRECT dependency direction (app -> buttons, never buttons -> app).

**welcome.py -- Extract from monolith lines 515-615:**

Create `inky_photo_frame/welcome.py` containing three functions extracted from `InkyPhotoFrame` methods:

1. `get_ip_address()` (lines 515-524) -- module-level function, remove `self`. Uses `socket` stdlib only.

2. `get_credentials()` (lines 526-537) -- module-level function, remove `self`. Uses `pathlib.Path` to read `/home/pi/.inky_credentials`.

3. `display_welcome(display, width, height)` (lines 539-615) -- module-level function. Takes explicit parameters:
   - `display`: the Inky display object (to call `display.set_image()` and `display.show()`)
   - `width`: int (display width)
   - `height`: int (display height)

   Replace `self.width` with `width`, `self.height` with `height`, `self.display` with `display`. Replace `self.get_ip_address()` with `get_ip_address()` and `self.get_credentials()` with `get_credentials()`.

**Imports for welcome.py:**
```python
import logging
import socket
from pathlib import Path
from PIL import Image, ImageDraw, ImageFont
```

**Critical rules for welcome.py:**
- No import from `config.py` needed (welcome.py uses only stdlib and PIL)
- The bare `except:` in font loading (line 553) should be preserved as-is (intentional graceful degradation)
- The `try/except TypeError` for `display.set_image()` (lines 611-614) is preserved for API compatibility
  </action>
  <verify>
Run: `python3 -c "from inky_photo_frame.photos import PhotoHandler, get_all_photos, load_history; print('photos.py OK')"` -- should print "photos.py OK".

Run: `python3 -c "from inky_photo_frame.buttons import BUTTONS_AVAILABLE, ButtonController; print(f'buttons.py OK, BUTTONS_AVAILABLE={BUTTONS_AVAILABLE}')"` -- should print "buttons.py OK, BUTTONS_AVAILABLE=False" (since gpiozero is not installed on dev machine).

Run: `python3 -c "from inky_photo_frame.welcome import display_welcome, get_ip_address, get_credentials; print('welcome.py OK')"` -- should print "welcome.py OK".
  </verify>
  <done>photos.py has PhotoHandler, get_all_photos(), load_history(). buttons.py has BUTTONS_AVAILABLE flag and ButtonController with constructor injection. welcome.py has display_welcome(), get_ip_address(), get_credentials() as module-level functions. No circular imports.</done>
</task>

</tasks>

<verification>
1. `ls inky_photo_frame/display.py inky_photo_frame/image_processor.py inky_photo_frame/photos.py inky_photo_frame/buttons.py inky_photo_frame/welcome.py` -- all 5 files exist
2. `python3 -c "from inky_photo_frame.display import DisplayManager, retry_on_error"` -- succeeds
3. `python3 -c "from inky_photo_frame.image_processor import process_image"` -- succeeds
4. `python3 -c "from inky_photo_frame.photos import PhotoHandler, get_all_photos, load_history"` -- succeeds
5. `python3 -c "from inky_photo_frame.buttons import BUTTONS_AVAILABLE, ButtonController"` -- succeeds
6. `python3 -c "from inky_photo_frame.welcome import display_welcome, get_ip_address, get_credentials"` -- succeeds
7. `grep -r "from inky_photo_frame.app" inky_photo_frame/display.py inky_photo_frame/image_processor.py inky_photo_frame/photos.py inky_photo_frame/buttons.py inky_photo_frame/welcome.py` -- returns NO matches (no circular imports to app.py)
8. `grep -r "from inky.auto" inky_photo_frame/display.py` -- only appears inside the `initialize()` method body, not at module level
</verification>

<success_criteria>
- All 5 leaf modules exist and are importable without hardware
- Each module imports only from config.py (or stdlib/third-party), never from app.py
- ButtonController and PhotoHandler use constructor injection (no type import of InkyPhotoFrame)
- process_image() takes explicit width/height/color_mode/is_spectra parameters
- display_welcome() takes explicit display/width/height parameters
- No circular imports detected
</success_criteria>

<output>
After completion, create `.planning/phases/03-module-extraction/03-02-SUMMARY.md`
</output>
