---
phase: 02-release-preparation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .github/release-notes-v2.0.0.md
autonomous: true
requirements:
  - RELS-01

must_haves:
  truths:
    - "A GitHub Release v2.0.0 draft exists on the repository"
    - "Release notes contain Highlights, Breaking Changes, Upgrade, and Full Changelog sections"
    - "Release notes reference GPIO fix (PR #3), module refactor, and test suite"
    - "The release is in draft state (not published)"
  artifacts:
    - path: ".github/release-notes-v2.0.0.md"
      provides: "Release notes source file for GitHub Release draft"
      contains: "## Highlights"
  key_links:
    - from: ".github/release-notes-v2.0.0.md"
      to: "GitHub Release v2.0.0 draft"
      via: "gh release create --draft --notes-file"
      pattern: "gh release"
---

<objective>
Draft comprehensive GitHub Release notes for v2.0.0 and create the release as a Draft on GitHub. The notes describe the full v2.0 release as if all work is complete (per user decision), to be validated and adjusted in the final phase before publication.

Purpose: Satisfy RELS-01 (GitHub Release v2.0 published with comprehensive release notes) by creating the draft now so release can be published immediately once validation passes.
Output: `.github/release-notes-v2.0.0.md` committed to repo + GitHub Release draft created via `gh release create`.
</objective>

<execution_context>
@/Users/mehdiguiard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mehdiguiard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-release-preparation/02-CONTEXT.md
@.planning/phases/02-release-preparation/02-RESEARCH.md
@.planning/phases/02-release-preparation/02-01-SUMMARY.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write release notes and create GitHub Release draft</name>
  <files>.github/release-notes-v2.0.0.md</files>
  <action>
Create `.github/` directory if it does not exist. Write `.github/release-notes-v2.0.0.md` with the release notes for v2.0.0.

**Structure per user decisions (4 main sections):**

1. **Highlights** (3-5 bullet points summarizing key changes):
   - GPIO fix for 13.3" displays (PR #3) -- button C now reads GPIO 25 instead of conflicting GPIO 16
   - Modular package architecture -- monolith split into 7 focused modules (config, display, image_processor, photos, buttons, welcome, app) with backward-compatible shim
   - Automated test suite -- pytest with hardware mocks, 70%+ coverage gate
   - CI pipeline -- GitHub Actions running ruff lint, ruff format, and pytest on every push/PR
   - Configurable photo rotation interval via CHANGE_INTERVAL_MINUTES

2. **Breaking Changes**:
   - "None" -- explain that v2.0 is fully backward-compatible: the entry-point shim (`inky_photo_frame.py`) continues to work identically, systemd service requires no path changes, `update.sh` handles the migration transparently

3. **Upgrade** (instructions for v1.x to v2.0):
   - For users with the CLI: `inky-photo-frame update`
   - For users without the CLI: `bash /home/pi/inky-photo-frame/update.sh`
   - Explain that update.sh automatically downloads the new package structure alongside the existing entry point
   - Note: no manual steps required, photos and configuration are preserved

4. **Full Changelog** (organized by category):
   - Added: Package structure, test suite, CI pipeline, hardware mocks, configurable interval
   - Changed: Monolith to modular architecture, entry point becomes shim
   - Fixed: GPIO 25 for 13.3" displays (PR #3)
   - Reference CHANGELOG.md for complete version history

**Tone:** Technical and factual (not marketing/narrative) per user decision.

**Important:** Write as if everything is complete (per user decision "draft the complete release notes now as if everything is done -- validate/adjust in final phase"). Use present tense for all features.

After writing the file, create the GitHub Release draft:

```bash
# Create .github directory if needed
mkdir -p .github

# Create the draft release
gh release create v2.0.0 \
  --draft \
  --title "Inky Photo Frame v2.0.0" \
  --notes-file .github/release-notes-v2.0.0.md \
  --target main
```

**Note on auto-tag behavior:** Per research, `gh release create` will auto-create a lightweight v2.0.0 tag pointing to HEAD of main. This is accepted per the research recommendation. The tag will be moved to the correct final commit at publication time with `git tag -f v2.0.0 <commit> && git push origin v2.0.0 --force`. Document this in the SUMMARY.
  </action>
  <verify>
1. File exists: `test -f .github/release-notes-v2.0.0.md && echo OK`
2. Required sections present: `grep -c '## Highlights\|## Breaking Changes\|## Upgrade\|## Full Changelog' .github/release-notes-v2.0.0.md` returns 4
3. Key references present: `grep -c 'PR #3\|GPIO 25\|module\|test suite' .github/release-notes-v2.0.0.md` returns >= 4
4. Draft release exists: `gh release view v2.0.0 --json isDraft -q '.isDraft'` returns `true`
5. Release title correct: `gh release view v2.0.0 --json name -q '.name'` returns "Inky Photo Frame v2.0.0"
6. No emoji in release notes: `grep -cP '[\x{1F300}-\x{1F9FF}]' .github/release-notes-v2.0.0.md` returns 0
  </verify>
  <done>
`.github/release-notes-v2.0.0.md` exists in the repository with all 4 required sections (Highlights, Breaking Changes, Upgrade, Full Changelog). A GitHub Release v2.0.0 draft is visible at the repository's releases page with the correct title and notes. The release is in draft state, not published. The auto-created v2.0.0 tag is documented as needing to be moved at final publication.
  </done>
</task>

</tasks>

<verification>
- `.github/release-notes-v2.0.0.md` exists with all 4 sections
- GitHub Release draft is visible via `gh release list` with "Draft" status
- Release notes reference GPIO fix (PR #3), module refactor, and test suite
- Tone is technical and factual, not marketing
- Upgrade instructions are clear and accurate
- Breaking Changes section explicitly states "None" with explanation
</verification>

<success_criteria>
A GitHub Release v2.0.0 draft exists on the repository with comprehensive release notes containing Highlights, Breaking Changes, Upgrade, and Full Changelog sections. The notes accurately describe all v2.0 changes in a technical, factual tone. The release is in draft state, ready to be published after final validation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-release-preparation/02-02-SUMMARY.md`
</output>
