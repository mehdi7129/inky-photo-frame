---
phase: 01-pre-flight-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inky_photo_frame.py
  - inky-photo-frame-cli
autonomous: true
requirements:
  - HYGN-01

must_haves:
  truths:
    - "PR #3 is merged and closed on GitHub with squash merge"
    - "The feature branch for PR #3 is deleted on the remote"
    - "GPIO 25 is used for 13.3 inch button C instead of GPIO 16"
    - "All GPIO pin assignments across all display sizes have no conflicts"
    - "Local uncommitted improvements to inky_photo_frame.py are committed as a separate commit after the PR merge"
  artifacts:
    - path: "inky_photo_frame.py"
      provides: "GPIO 25 fix for 13.3 inch display plus local improvements"
      contains: "'button_c': 25"
  key_links:
    - from: "PR #3 on GitHub"
      to: "inky_photo_frame.py in main branch"
      via: "gh pr merge --squash"
      pattern: "button_c.*25"
---

<objective>
Merge PR #3 (GPIO 25 hardware fix for 13.3" displays) into main via squash merge, then reconcile the local uncommitted improvements that overlap with the PR changes.

Purpose: PR #3 fixes a real hardware conflict where GPIO 16 was used for both SPI chip select and button C on 13.3" displays. Merging via GitHub gives proper attribution and cleanly closes the PR. The local working tree already contains a superset of these changes plus additional features (CHANGE_INTERVAL_MINUTES, updated docstring) that must be preserved and committed separately.

Output: PR #3 closed on GitHub, GPIO fix integrated, local improvements committed on top.
</objective>

<execution_context>
@/Users/mehdiguiard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mehdiguiard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pre-flight-hygiene/01-RESEARCH.md
@.planning/phases/01-pre-flight-hygiene/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Squash merge PR #3 via GitHub CLI</name>
  <files>inky_photo_frame.py</files>
  <action>
Execute the PR #3 merge workflow in this exact sequence. Each step MUST succeed before proceeding.

**Step 0 — Verify starting state:**
- Run `git status` to confirm modified files: `inky_photo_frame.py`, `inky-photo-frame-cli`
- Run `gh pr view 3 --json state,mergeable,mergeStateStatus` to confirm PR is OPEN and MERGEABLE

**Step 1 — Stash uncommitted local changes:**
- Run `git stash push -m "pre-PR3-merge local changes" -- inky_photo_frame.py inky-photo-frame-cli`
- This preserves the local improvements (CHANGE_INTERVAL_MINUTES, docstring update, CLI reset-password) without committing them

**Step 2 — Push local planning commits to origin:**
- Run `git push origin main`
- The local branch is ~8 commits ahead of origin (all planning docs). Origin must be current before the squash merge.

**Step 3 — Squash merge PR #3:**
- Run `gh pr merge 3 --squash --delete-branch`
- This creates a single clean commit on remote main, closes PR #3, and deletes the feature branch.
- The squash merge commit will contain the GPIO 25 fix per user decision.

**Step 4 — Pull the merge commit:**
- Run `git pull origin main`
- This brings the squash merge commit into the local main branch.

**Step 5 — Restore stashed changes:**
- Run `git stash pop`
- If conflicts arise in `inky_photo_frame.py`, resolve by keeping the local version since it is a superset of PR #3 changes. The GPIO 25 fix is already present in both the PR and the local changes.

**Step 6 — Verify GPIO 25 fix is present:**
- Run `grep -n "button_c.*25" inky_photo_frame.py` to confirm GPIO 25 assignment for 13.3" display
- Run `grep -n "gpio_pins\|button_[abcd]" inky_photo_frame.py` to audit ALL GPIO pin assignments

**GPIO pin audit checklist (verify these exact values):**
| Display | Button A | Button B | Button C | Button D |
|---------|----------|----------|----------|----------|
| spectra_7.3 | GPIO 5 | GPIO 6 | GPIO 16 | GPIO 24 |
| classic_7.3 | GPIO 5 | GPIO 6 | GPIO 16 | GPIO 24 |
| spectra_13.3 | GPIO 5 | GPIO 6 | GPIO 25 | GPIO 24 |

GPIO 16 is acceptable for 7.3" models (no SPI conflict). Only 13.3" must use GPIO 25.

**Step 7 — Verify PR is closed:**
- Run `gh pr view 3 --json state` and confirm state is "MERGED"
- Run `gh pr view 3 --json headRefName` and confirm the branch was deleted (or check with `git branch -r`)

**CRITICAL: Do NOT use `git add -A` or `git add .` at any point. Only stage specific files.**

**Note on inky-photo-frame-cli:** The CLI file has uncommitted changes (reset-password feature) unrelated to Phase 1 scope. After stash pop, leave these changes uncommitted. Do NOT stage or commit the CLI changes in this task. Per research recommendation, the reset-password feature is out of Phase 1 scope.
  </action>
  <verify>
- `gh pr view 3 --json state` returns `"state": "MERGED"`
- `git log --oneline -5` shows the squash merge commit from PR #3
- `grep "button_c.*25" inky_photo_frame.py` returns a match for the 13.3" display config
- `git stash list` is empty (stash was popped)
  </verify>
  <done>PR #3 is squash-merged and closed on GitHub. The feature branch is deleted. GPIO 25 is confirmed for 13.3" button C. Local working tree changes are restored from stash.</done>
</task>

<task type="auto">
  <name>Task 2: Commit local improvements as separate commit</name>
  <files>inky_photo_frame.py</files>
  <action>
After PR #3 is merged, the working tree still has local uncommitted changes to `inky_photo_frame.py` that go beyond the PR scope. These include CHANGE_INTERVAL_MINUTES feature, updated docstring, and possibly minor formatting differences. Commit these as a distinct commit to preserve attribution.

**Step 1 — Inspect the remaining diff:**
- Run `git diff inky_photo_frame.py` to see what local changes remain after the merge
- Verify the diff contains ONLY the expected additional features (CHANGE_INTERVAL_MINUTES, docstring update)
- If any unexpected changes appear, stop and report them

**Step 2 — Stage and commit only inky_photo_frame.py:**
- Run `git add inky_photo_frame.py`
- Do NOT stage `inky-photo-frame-cli` (the reset-password feature is out of Phase 1 scope)
- Commit with a descriptive message:
  ```
  feat: add configurable photo change interval and update docstring

  Add CHANGE_INTERVAL_MINUTES constant (default 0 = daily mode,
  >0 = change every N minutes). Update module docstring to reflect
  current feature set including multi-display support and color modes.

  These changes were developed locally alongside PR #3 and are
  committed separately for clear attribution.
  ```

**Step 3 — Verify clean state for inky_photo_frame.py:**
- Run `git status` to confirm `inky_photo_frame.py` is no longer in the modified list
- `inky-photo-frame-cli` should still show as modified (expected, out of scope)

**IMPORTANT:** If `git diff inky_photo_frame.py` shows NO changes (meaning the PR merge already incorporated everything), skip this task and note it in the summary. The stash pop may have resolved cleanly with no remaining diff.
  </action>
  <verify>
- `git log --oneline -1` shows the local improvements commit (or task was skipped if no diff remained)
- `git diff inky_photo_frame.py` returns empty (no uncommitted changes to this file)
- `git status` shows `inky-photo-frame-cli` still modified (intentionally left)
  </verify>
  <done>Local improvements to inky_photo_frame.py are committed as a separate, properly attributed commit. The CLI file remains uncommitted as intended.</done>
</task>

</tasks>

<verification>
1. `gh pr list --state merged --json number,title | grep '"number":3'` confirms PR #3 is merged
2. `git log --oneline -10` shows: local improvements commit (if any), PR #3 squash merge, planning commits
3. `grep "button_c.*25" inky_photo_frame.py` returns the 13.3" GPIO 25 assignment
4. `git diff --name-only` shows only `inky-photo-frame-cli` (expected out-of-scope modification)
5. No `__pycache__` changes, no doc removals (those are Plan 02)
</verification>

<success_criteria>
- PR #3 is MERGED on GitHub (not just changes in code)
- Feature branch is deleted from remote
- GPIO 25 confirmed for 13.3" button C via grep
- All GPIO pins audited with no conflicts across display sizes
- Local improvements committed separately (or noted as unnecessary)
- Working tree is clean except for inky-photo-frame-cli (intentional)
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-flight-hygiene/01-01-SUMMARY.md`
</output>
