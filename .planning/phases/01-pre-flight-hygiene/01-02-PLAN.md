---
phase: 01-pre-flight-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .gitignore
  - SUMMARY.md
  - COLOR_CALIBRATION.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements:
  - HYGN-02
  - HYGN-03

must_haves:
  truths:
    - "git ls-files shows no __pycache__ or .pyc files tracked"
    - ".gitignore contains comprehensive Python patterns including testing and packaging artifacts"
    - "SUMMARY.md does not exist in the repository"
    - "COLOR_CALIBRATION.md does not exist in the repository"
    - "No broken references to deleted files remain in planning docs"
  artifacts:
    - path: ".gitignore"
      provides: "Comprehensive Python gitignore with project-specific patterns"
      contains: ".pytest_cache/"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Updated directory tree without obsolete files"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Updated concerns with deletion marked complete"
  key_links:
    - from: ".gitignore"
      to: "Phase 5 CI artifacts"
      via: "Ignore patterns for .pytest_cache, htmlcov, .coverage"
      pattern: "\\.pytest_cache"
---

<objective>
Expand .gitignore to comprehensive Python coverage, remove __pycache__ from tracking if present, and delete obsolete documentation files (SUMMARY.md, COLOR_CALIBRATION.md) with reference cleanup.

Purpose: A clean .gitignore prevents future accidents (compiled files, test artifacts, IDE configs). Removing obsolete docs eliminates confusion for contributors. Both tasks prepare the repo for the structural work in Phase 3 and the CI setup in Phase 5.

Output: Comprehensive .gitignore, no tracked cache files, no obsolete docs, updated planning references.
</objective>

<execution_context>
@/Users/mehdiguiard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mehdiguiard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pre-flight-hygiene/01-RESEARCH.md
@.planning/phases/01-pre-flight-hygiene/01-CONTEXT.md
@.planning/phases/01-pre-flight-hygiene/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand .gitignore and remove __pycache__ from tracking</name>
  <files>.gitignore</files>
  <action>
**Step 1 — Read current .gitignore:**
- Read `.gitignore` to understand existing patterns (it already has `__pycache__/` and `*.py[cod]`)

**Step 2 — Check for tracked __pycache__ files:**
- Run `git ls-files | grep __pycache__` to check if any are tracked
- If results found: run `git rm -r --cached __pycache__/` to untrack them (keeps on disk, removes from git)
- If no results: skip removal (research found this is likely the case)

**Step 3 — Scan for other files that should not be tracked:**
- Run `git ls-files | grep -E '\.(pyc|pyo|egg-info)$'` to check for compiled files
- Run `git ls-files | grep -E '(\.env|\.vscode|\.idea|\.DS_Store)'` to check for IDE/env files
- Report any findings. Remove tracked files that match .gitignore patterns using `git rm --cached`.

**Step 4 — Expand .gitignore:**
Read the current `.gitignore` and ADD the following missing Python patterns (merge with existing, do not duplicate). Keep all existing project-specific patterns (especially the `*.jpg`, `*.png` image ignores which are intentional for the photos directory).

Patterns to add (grouped by category, with comments):

```gitignore
# Distribution / packaging
*.egg-info/
*.egg
dist/
build/
sdist/
develop-eggs/
eggs/
wheels/
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt

# Testing
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Environments
.env
.env.*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
```

**Do NOT add** patterns that could conflict with project files:
- Do NOT add `*.cfg` (could match logrotate.conf if renamed)
- Do NOT add `local_settings.py` (Django-specific)
- Do NOT add `lib/` or `lib64/` (too generic, could match project directories in future)
- Do NOT add `var/` or `parts/` (too generic)

**Step 5 — Commit the .gitignore update:**
- Stage: `git add .gitignore`
- If __pycache__ was untracked in Step 2, the `git rm --cached` changes are already staged
- Commit with message:
  ```
  chore: expand .gitignore with comprehensive Python patterns

  Add standard patterns for distribution packaging, testing artifacts
  (pytest, coverage, tox), IDE files, and OS metadata. Existing
  project-specific patterns (image files, __pycache__) preserved.

  Prepares for Phase 5 CI setup which will generate .pytest_cache/,
  htmlcov/, and .coverage files.
  ```
  </action>
  <verify>
- `git ls-files | grep -E '__pycache__|\.pyc'` returns empty (no compiled files tracked)
- `grep '.pytest_cache' .gitignore` returns a match
- `grep 'htmlcov' .gitignore` returns a match
- `grep '.coverage' .gitignore` returns a match
- `grep '*.egg-info' .gitignore` returns a match (note: grep the literal pattern)
- The existing `*.jpg`, `*.png` patterns are still present in .gitignore
  </verify>
  <done>.gitignore is expanded with comprehensive Python patterns. No __pycache__ or .pyc files are tracked. Existing project-specific ignore rules are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove obsolete documentation and update references</name>
  <files>SUMMARY.md, COLOR_CALIBRATION.md, .planning/codebase/STRUCTURE.md, .planning/codebase/CONCERNS.md</files>
  <action>
**Step 1 — Verify files exist and scan for references:**
- Confirm `SUMMARY.md` and `COLOR_CALIBRATION.md` exist: `ls -la SUMMARY.md COLOR_CALIBRATION.md`
- Run `grep -rn "SUMMARY\.md\|COLOR_CALIBRATION\.md" --include="*.md" .` to find all references
- Cross-reference results with the research scan table. Expected references:
  - `.planning/codebase/STRUCTURE.md` (directory tree listing) — MUST update
  - `.planning/codebase/CONCERNS.md` (concern description) — MUST update
  - `.planning/PROJECT.md` (checklist item) — mark as done
  - `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` (requirement/criterion text) — leave as-is
  - `.planning/research/*` (historical research findings) — leave as-is
- If any NEW references are found in source code (`.py`, `.sh`) or `README.md`, fix them before deleting

**Step 2 — Check for other potentially obsolete documentation:**
- Per user decision: "Flag any other potentially obsolete documentation files found during cleanup"
- Check `INSTALLATION_GUIDE.md` — the STRUCTURE.md marks it as "candidate for removal in Phase 1"
- Check if README.md adequately covers installation (if so, INSTALLATION_GUIDE.md may be obsolete)
- Do NOT delete additional files — only flag them in the commit message or summary for user review

**Step 3 — Remove the obsolete files from git:**
- Run `git rm SUMMARY.md COLOR_CALIBRATION.md`
- This removes from both tracking and working tree

**Step 4 — Update .planning/codebase/STRUCTURE.md:**
- Read the file
- Remove the lines listing `SUMMARY.md` and `COLOR_CALIBRATION.md` from the directory tree (approximately lines 42-44)
- Remove the comment "(to be removed in Phase 1)" from those areas
- Also remove the `__pycache__/` line from the directory tree if it was listed with "(committed by mistake, fix in Phase 1)" since Task 1 handled that

**Step 5 — Update .planning/codebase/CONCERNS.md:**
- Read the file
- Find the section referencing SUMMARY.md and COLOR_CALIBRATION.md (around lines 219-220)
- Update the text to indicate these files have been removed (past tense)
- Example: Change "are obsolete and should be removed" to "were removed in Phase 1"

**Step 6 — Commit with detailed message:**
- Stage: `git add SUMMARY.md COLOR_CALIBRATION.md .planning/codebase/STRUCTURE.md .planning/codebase/CONCERNS.md`
- Note: `git rm` already stages the removals, but the planning file updates need `git add`
- Commit with message per user decision ("Use detailed commit messages explaining what was removed and why"):
  ```
  chore: remove obsolete SUMMARY.md and COLOR_CALIBRATION.md

  SUMMARY.md was a v1.1.7 release summary in French, now superseded
  by CHANGELOG.md which covers the same content more comprehensively.

  COLOR_CALIBRATION.md was a v1.0.1 color tuning guide, now outdated
  since v1.1.0 introduced the 3-mode color system (pimoroni,
  spectra_palette, warmth_boost) with runtime button switching.
  README.md covers current color configuration.

  Updated .planning/codebase/STRUCTURE.md and CONCERNS.md to reflect
  the removals. No source code or README references were found.

  Closes requirement HYGN-03.
  ```

**CRITICAL: Do NOT use `git add -A` or `git add .`. Only stage specific files.**
  </action>
  <verify>
- `ls SUMMARY.md 2>/dev/null` returns nothing (file gone)
- `ls COLOR_CALIBRATION.md 2>/dev/null` returns nothing (file gone)
- `git ls-files | grep -E 'SUMMARY\.md|COLOR_CALIBRATION\.md'` returns empty
- `grep -c "to be removed in Phase 1" .planning/codebase/STRUCTURE.md` returns 0
- `git log --oneline -1` shows the removal commit with descriptive message
  </verify>
  <done>SUMMARY.md and COLOR_CALIBRATION.md are deleted from the repository. Planning docs are updated to reflect the removal. No broken references remain.</done>
</task>

</tasks>

<verification>
1. `git ls-files | grep -E '__pycache__|\.pyc'` returns empty
2. `grep '.pytest_cache' .gitignore` returns a match
3. `ls SUMMARY.md COLOR_CALIBRATION.md 2>/dev/null` returns nothing
4. `grep -rn "SUMMARY\.md" .planning/codebase/STRUCTURE.md` shows no tree listing for it
5. `git log --oneline -5` shows both commits (gitignore expansion + doc removal)
6. `git status` shows only `inky-photo-frame-cli` as modified (carried from Plan 01)
</verification>

<success_criteria>
- No __pycache__ or .pyc files tracked by git
- .gitignore contains patterns for: packaging (*.egg-info, dist/, build/), testing (.pytest_cache/, .coverage, htmlcov/), IDE (.vscode/, .idea/), OS (.DS_Store)
- SUMMARY.md and COLOR_CALIBRATION.md are absent from the repository
- .planning/codebase/STRUCTURE.md directory tree no longer lists the deleted files
- .planning/codebase/CONCERNS.md reflects the completed removal
- Any other potentially obsolete docs flagged for user review
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-flight-hygiene/01-02-SUMMARY.md`
</output>
